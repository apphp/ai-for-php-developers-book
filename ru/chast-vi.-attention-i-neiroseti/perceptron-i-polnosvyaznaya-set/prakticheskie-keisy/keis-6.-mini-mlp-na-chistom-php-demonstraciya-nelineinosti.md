# Кейс 6. Мини-MLP на чистом PHP (демонстрация нелинейности)

Можно сделать простой пример с ReLU:

```
private function relu(float $z): float
{
    return max(0, $z);
}
```

И показать, как два линейных слоя без ReLU схлопываются в один.

Это очень сильный концептуальный момент для главы.
